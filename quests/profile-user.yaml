# Waypoint source-derived catalog Quest from commands/gsd/profile-user.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.source_port.

schema_version: 1
slug: profile-user
name: Profile User Quest
description: >
  Waypoint catalog Quest port of gsd:profile-user for generate developer behavior and preference profile artifacts with consent gates.
workflow: workflows/waypoint/profile-user.yaml

recipes:
  - waypoint-user-profiler

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: Profile User Quest loop
          phases:
            - phase_key: "10"
              phase_slug: profile_user
              lifecycle_phase: profile_user
              plans:
                - plan_ref: profile-user-intake
                  title: Capture inputs and current context
                  wave: 10
                - plan_ref: profile-user-run
                  title: Run the mapped Waypoint Recipes
                  wave: 20
                - plan_ref: profile-user-report
                  title: Produce operator-facing output and next actions
                  wave: 30

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - waypoint-port
    - quest-catalog
  source_port:
    scope: command_informed_sub_quest
    category: utility_inspection
    role: utility / inspection Quest
    source_command: commands/gsd/profile-user.md
    source_command_name: gsd:profile-user
    intent: Analyze sessions or questionnaire responses to produce user preference/profile artifacts.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
