# Waypoint source-derived catalog Quest from commands/gsd/sketch.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.source_port.

schema_version: 1
slug: sketch
name: Sketch Quest
description: >
  Waypoint catalog Quest port of gsd:sketch for explore UI/design directions through throwaway mockups before implementation.
workflow: workflows/waypoint/sketch.yaml

recipes:
  - waypoint-ui-researcher
  - waypoint-ui-auditor

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: Sketch Quest loop
          phases:
            - phase_key: "10"
              phase_slug: sketch
              lifecycle_phase: sketch
              plans:
                - plan_ref: sketch-intake
                  title: Capture inputs and current context
                  wave: 10
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint
                - plan_ref: sketch-run-ui-researcher
                  title: Run Ui Researcher
                  wave: 20
                  metadata:
                    waypoint:
                      node:
                        type: recipe
                      recipe:
                        slug: waypoint-ui-researcher
                - plan_ref: sketch-run-ui-auditor
                  title: Run Ui Auditor
                  wave: 21
                  metadata:
                    waypoint:
                      node:
                        type: recipe
                      recipe:
                        slug: waypoint-ui-auditor
                - plan_ref: sketch-report
                  title: Produce operator-facing output and next actions
                  wave: 30
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - waypoint-port
    - quest-catalog
  source_port:
    scope: command_informed_sub_quest
    category: utility_inspection
    role: utility / inspection Quest
    source_command: commands/gsd/sketch.md
    source_command_name: gsd:sketch
    intent: Produce and compare design sketches, then hand off viable directions to planning.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
