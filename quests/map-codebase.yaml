# Waypoint source-derived catalog Quest from commands/gsd/map-codebase.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.source_port.

schema_version: 1
slug: map-codebase
name: Map Codebase Quest
description: >
  Waypoint catalog Quest port of gsd:map-codebase for parallel codebase analysis producing structured codebase intelligence.
workflow: workflows/waypoint/map-codebase.yaml

recipes:
  - waypoint-codebase-mapper

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: Map Codebase Quest loop
          phases:
            - phase_key: "10"
              phase_slug: map_codebase
              lifecycle_phase: map_codebase
              plans:
                - plan_ref: map-codebase-intake
                  title: Capture inputs and current context
                  wave: 10
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint
                - plan_ref: map-codebase-run
                  title: Run Codebase Mapper
                  wave: 20
                  metadata:
                    waypoint:
                      node:
                        type: recipe
                      recipe:
                        slug: waypoint-codebase-mapper
                - plan_ref: map-codebase-report
                  title: Produce operator-facing output and next actions
                  wave: 30
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - waypoint-port
    - quest-catalog
  source_port:
    scope: command_informed_sub_quest
    category: utility_inspection
    role: utility / inspection Quest
    source_command: commands/gsd/map-codebase.md
    source_command_name: gsd:map-codebase
    intent: Map architecture, technology, quality concerns, and project structure into reusable codebase docs.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
