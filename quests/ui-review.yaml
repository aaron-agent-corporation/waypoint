# Waypoint source-derived catalog Quest from commands/gsd/ui-review.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.source_port.

schema_version: 1
slug: ui-review
name: UI Review Quest
description: >
  Waypoint catalog Quest port of gsd:ui-review for retroactive UI and visual quality audit.
workflow: workflows/waypoint/ui-review.yaml

recipes:
  - waypoint-ui-auditor
  - waypoint-ui-checker

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: UI Review Quest loop
          phases:
            - phase_key: "10"
              phase_slug: ui_review
              lifecycle_phase: ui_review
              plans:
                - plan_ref: ui-review-intake
                  title: Capture inputs and current context
                  wave: 10
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint
                - plan_ref: ui-review-run-ui-auditor
                  title: Run Ui Auditor
                  wave: 20
                  metadata:
                    waypoint:
                      node:
                        type: recipe
                      recipe:
                        slug: waypoint-ui-auditor
                - plan_ref: ui-review-run-ui-checker
                  title: Run Ui Checker
                  wave: 21
                  metadata:
                    waypoint:
                      node:
                        type: recipe
                      recipe:
                        slug: waypoint-ui-checker
                - plan_ref: ui-review-report
                  title: Produce operator-facing output and next actions
                  wave: 30
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - waypoint-port
    - quest-catalog
  source_port:
    scope: command_informed_sub_quest
    category: audit_review
    role: audit / review sub-Quest
    source_command: commands/gsd/ui-review.md
    source_command_name: gsd:ui-review
    intent: Evaluate UI implementation against visual-quality pillars and produce remediation guidance.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
