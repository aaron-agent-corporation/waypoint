# Waypoint source-derived catalog Quest from commands/gsd/extract-learnings.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.source_port.

schema_version: 1
slug: extract-learnings
name: Extract Learnings Quest
description: >
  Waypoint catalog Quest port of gsd:extract-learnings for extract decisions, lessons, patterns, and surprises from completed phase artifacts.
workflow: workflows/waypoint/extract-learnings.yaml

recipes:
  - waypoint-doc-synthesizer
  - waypoint-pattern-mapper

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: Extract Learnings Quest loop
          phases:
            - phase_key: "10"
              phase_slug: extract_learnings
              lifecycle_phase: extract_learnings
              plans:
                - plan_ref: extract-learnings-intake
                  title: Capture inputs and current context
                  wave: 10
                - plan_ref: extract-learnings-run
                  title: Run the mapped Waypoint Recipes
                  wave: 20
                - plan_ref: extract-learnings-report
                  title: Produce operator-facing output and next actions
                  wave: 30

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - waypoint-port
    - quest-catalog
  source_port:
    scope: command_informed_sub_quest
    category: utility_inspection
    role: utility / inspection Quest
    source_command: commands/gsd/extract-learnings.md
    source_command_name: gsd:extract-learnings
    intent: Turn completed phase artifacts into reusable learning notes and patterns.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
