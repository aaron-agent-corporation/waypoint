# Waypoint source-derived catalog Quest from commands/gsd/review-backlog.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.source_port.

schema_version: 1
slug: review-backlog
name: Review Backlog Quest
description: >
  Waypoint catalog Quest port of gsd:review-backlog for review and promote backlog items into active planning.
workflow: workflows/waypoint/review-backlog.yaml

recipes:
  - waypoint-roadmapper
  - waypoint-planner

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: Review Backlog Quest loop
          phases:
            - phase_key: "10"
              phase_slug: review_backlog
              lifecycle_phase: review_backlog
              plans:
                - plan_ref: review-backlog-intake
                  title: Capture inputs and current context
                  wave: 10
                - plan_ref: review-backlog-run
                  title: Run the mapped Waypoint Recipes
                  wave: 20
                - plan_ref: review-backlog-report
                  title: Produce operator-facing output and next actions
                  wave: 30

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - waypoint-port
    - quest-catalog
  source_port:
    scope: command_informed_sub_quest
    category: audit_review
    role: audit / review sub-Quest
    source_command: commands/gsd/review-backlog.md
    source_command_name: gsd:review-backlog
    intent: Inspect backlog candidates and route them to promote, keep, or remove decisions.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
