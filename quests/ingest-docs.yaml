# Waypoint source-derived catalog Quest from commands/gsd/ingest-docs.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.source_port.

schema_version: 1
slug: ingest-docs
name: Ingest Docs Quest
description: >
  Waypoint catalog Quest port of gsd:ingest-docs for bootstrap or merge planning setup from existing ADRs, PRDs, specs, and docs.
workflow: workflows/waypoint/ingest-docs.yaml

recipes:
  - waypoint-doc-classifier
  - waypoint-doc-synthesizer

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: Ingest Docs Quest loop
          phases:
            - phase_key: "10"
              phase_slug: ingest_docs
              lifecycle_phase: ingest_docs
              plans:
                - plan_ref: ingest-docs-intake
                  title: Capture inputs and current context
                  wave: 10
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint
                - plan_ref: ingest-docs-run-doc-classifier
                  title: Run Doc Classifier
                  wave: 20
                  metadata:
                    waypoint:
                      node:
                        type: recipe
                      recipe:
                        slug: waypoint-doc-classifier
                - plan_ref: ingest-docs-run-doc-synthesizer
                  title: Run Doc Synthesizer
                  wave: 21
                  metadata:
                    waypoint:
                      node:
                        type: recipe
                      recipe:
                        slug: waypoint-doc-synthesizer
                - plan_ref: ingest-docs-report
                  title: Produce operator-facing output and next actions
                  wave: 30
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - waypoint-port
    - quest-catalog
  source_port:
    scope: command_informed_sub_quest
    category: utility_inspection
    role: sub-Quest
    source_command: commands/gsd/ingest-docs.md
    source_command_name: gsd:ingest-docs
    intent: Classify existing documents and synthesize them into Waypoint planning artifacts.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
