# Waypoint source-derived catalog Quest from commands/gsd/eval-review.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.source_port.

schema_version: 1
slug: eval-review
name: Evaluation Review Quest
description: >
  Waypoint catalog Quest port of gsd:eval-review for audit AI/evaluation coverage for a completed AI phase.
workflow: workflows/waypoint/eval-review.yaml

recipes:
  - waypoint-eval-auditor
  - waypoint-eval-planner

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: Evaluation Review Quest loop
          phases:
            - phase_key: "10"
              phase_slug: eval_review
              lifecycle_phase: eval_review
              plans:
                - plan_ref: eval-review-intake
                  title: Capture inputs and current context
                  wave: 10
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint
                - plan_ref: eval-review-run-eval-auditor
                  title: Run Eval Auditor
                  wave: 20
                  metadata:
                    waypoint:
                      node:
                        type: recipe
                      recipe:
                        slug: waypoint-eval-auditor
                - plan_ref: eval-review-run-eval-planner
                  title: Run Eval Planner
                  wave: 21
                  metadata:
                    waypoint:
                      node:
                        type: recipe
                      recipe:
                        slug: waypoint-eval-planner
                - plan_ref: eval-review-report
                  title: Produce operator-facing output and next actions
                  wave: 30
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - waypoint-port
    - quest-catalog
  source_port:
    scope: command_informed_sub_quest
    category: audit_review
    role: audit / review sub-Quest
    source_command: commands/gsd/eval-review.md
    source_command_name: gsd:eval-review
    intent: Score evaluation coverage, identify gaps, and produce remediation planning.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
