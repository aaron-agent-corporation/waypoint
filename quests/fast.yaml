# Waypoint source-derived catalog Quest from commands/gsd/fast.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.source_port.

schema_version: 1
slug: fast
name: Fast Quest
description: >
  Waypoint catalog Quest port of gsd:fast for trivial task execution path with minimal planning overhead.
workflow: workflows/waypoint/fast.yaml

recipes:
  - waypoint-executor

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: Fast Quest loop
          phases:
            - phase_key: "10"
              phase_slug: fast
              lifecycle_phase: fast
              plans:
                - plan_ref: fast-intake
                  title: Capture inputs and current context
                  wave: 10
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint
                - plan_ref: fast-run
                  title: Run Executor
                  wave: 20
                  metadata:
                    waypoint:
                      node:
                        type: recipe
                      recipe:
                        slug: waypoint-executor
                - plan_ref: fast-report
                  title: Produce operator-facing output and next actions
                  wave: 30
                  metadata:
                    waypoint:
                      node:
                        type: checkpoint

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - waypoint-port
    - quest-catalog
  source_port:
    scope: command_informed_sub_quest
    category: utility_inspection
    role: utility / inspection Quest
    source_command: commands/gsd/fast.md
    source_command_name: gsd:fast
    intent: Execute very small tasks inline while preserving basic Waypoint provenance.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
