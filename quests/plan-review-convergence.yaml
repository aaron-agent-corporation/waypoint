# Waypoint GSD catalog Quest port from commands/gsd/plan-review-convergence.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.gsd_port.

schema_version: 1
slug: plan-review-convergence
name: Plan Review Convergence Quest
description: >
  Waypoint catalog Quest port of gsd:plan-review-convergence for iterative plan-review convergence loop until high concerns are resolved.
workflow: workflows/gsd/plan-review-convergence.yaml

recipes:
  - gsd-planner
  - gsd-plan-checker

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: Plan Review Convergence Quest loop
          phases:
            - phase_key: "10"
              phase_slug: plan_review_convergence
              lifecycle_phase: plan_review_convergence
              plans:
                - plan_ref: plan-review-convergence-intake
                  title: Capture inputs and current context
                  wave: 10
                - plan_ref: plan-review-convergence-run
                  title: Run the mapped Waypoint Recipes
                  wave: 20
                - plan_ref: plan-review-convergence-report
                  title: Produce operator-facing output and next actions
                  wave: 30

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - gsd-port
    - quest-catalog
  gsd_port:
    scope: command_informed_sub_quest
    category: audit_review
    role: audit / review sub-Quest
    source_command: commands/gsd/plan-review-convergence.md
    source_command_name: gsd:plan-review-convergence
    intent: Alternate planning and external review until the phase plan converges or escalates.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
