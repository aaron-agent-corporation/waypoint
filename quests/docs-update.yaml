# Waypoint source-derived catalog Quest from commands/gsd/docs-update.md.
#
# Current Quest schema has no first-class command graph field, so command
# mapping and orchestration intent are preserved in metadata.source_port.

schema_version: 1
slug: docs-update
name: Docs Update Quest
description: >
  Waypoint catalog Quest port of gsd:docs-update for generate or verify project documentation against the codebase.
workflow: workflows/waypoint/docs-update.yaml

recipes:
  - waypoint-doc-writer
  - waypoint-doc-verifier

scaffolds:
  workstreams:
    - key: delivery
      name: Delivery
      milestones:
        - version_label: v1
          title: Docs Update Quest loop
          phases:
            - phase_key: "10"
              phase_slug: docs_update
              lifecycle_phase: docs_update
              plans:
                - plan_ref: docs-update-intake
                  title: Capture inputs and current context
                  wave: 10
                - plan_ref: docs-update-run
                  title: Run the mapped Waypoint Recipes
                  wave: 20
                - plan_ref: docs-update-report
                  title: Produce operator-facing output and next actions
                  wave: 30

metadata:
  source:
    project: get-shit-done-cc
    local_reference: /Users/aaronwhaley/Downloads/get-shit-done-main
    license: MIT
  tags:
    - waypoint-port
    - quest-catalog
  source_port:
    scope: command_informed_sub_quest
    category: utility_inspection
    role: sub-Quest
    source_command: commands/gsd/docs-update.md
    source_command_name: gsd:docs-update
    intent: Update project docs through writer and verifier recipes while preserving source-grounding expectations.
    operator_interaction: command arguments, review gates, and follow-up decisions when required
    runtime_notes:
      - Quest and Recipe are Waypoint primitives; workflow YAML remains the internal execution substrate.
      - This command is modeled as a catalog Quest using only current schema fields.
      - Detailed command flags and execution semantics remain metadata/docs until first-class runtime support is added.
